{
  "name": "eth-nft-game",
  "version": "1.0.0",
  "description": "Creating NFT Game",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "contract": "yarn workspace contract",
    "client": "NODE_OPTIONS=--openssl-legacy-provider yarn workspace client",
    "test": "yarn workspace contract test"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.2.0"
  },
  "devDependencies": {
    "hardhat": "^2.22.18"
  }
}
